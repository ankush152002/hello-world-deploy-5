apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world-app5
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-world-app5
  template:
    metadata:
      labels:
        app: hello-world-app5
    spec:
      containers:
      - name: hello-world-app5
        image: us-central1-docker.pkg.dev/end-to-end-project-438607/hello-world-repo/hello-world-app5:latest
        ports:
        - containerPort: 3004
